{
  "name": "auto-gear",
  "version": "2.0.0",
  "private": true,
  "description": "Model cap + tier routing for Claude Code subagents.",
  "license": "MIT",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "bench": "node benchmarks/route.js"
  }
}
